local AllowedInstances = {142823291, 168556275}

if not table.find(AllowedInstances, game.PlaceId) then
    local player = game.Players.LocalPlayer
    if player then
        player:Kick("This script only works for MM2!")
    end
    return
end
